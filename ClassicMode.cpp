#include "ClassicMode.h"

ClassicMode::ClassicMode() {
  board = NULL;
}

ClassicMode::ClassicMode(GenerateBoard *b){
  board = b;
}

int ClassMode::iterateThroughBoard() {
  int board = &b->getBoard();
}

int countNumMeighbors(int cell) {
  //1. Iterate through board and get current cell
  int currentCell = board[i][j];

  //2. need to find out location of cell
  if ((i == 0 && j == 0) || (i == 0 && j == //board row size - 1) || (i == 0 && j == 0) || (i == 0 && j == 0)) { //all of the cases for the corners

  }

int nextGenStatus(int numNeighbors) {

}
